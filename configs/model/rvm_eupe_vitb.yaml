# Primary model: EUPE ViT-B/16 encoder (768d)
arch: vitb
weights: /home/farm/.cache/torch/hub/checkpoints/EUPE-ViT-B.pt
pretrained: true
mask_ratio: 0.95
num_source_frames: 4
bptt_truncate: true
decoder_dim: 512
decoder_heads: 16
decoder_blocks: 8
gru_heads: 12
gru_blocks: 1
